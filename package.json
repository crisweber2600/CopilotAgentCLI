{
  "name": "copilot-cli",
  "version": "0.32.0",
  "description": "Standalone GitHub Copilot CLI",
  "license": "SEE LICENSE IN ../LICENSE.txt",
  "private": false,
  "type": "commonjs",
  "bin": {
    "copilot-cli": "./dist/cli/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "cli": "node dist/cli/index.js",
    "test": "vitest run",
    "lint": "eslint src/ tests/ --ext .ts",
    "format": "prettier --write src/ tests/ --ignore-path .gitignore"
  },
  "dependencies": {
    "open": "^10.1.2"
  },
  "devDependencies": {
    "@types/node": "^22.16.3",
    "ajv": "^8.12.0",
    "eslint": "^8.57.0",
    "lint-staged": "^15.2.0",
    "prettier": "^3.3.0",
    "tsx": "^4.20.5",
    "typescript": "^5.8.3",
    "vitest": "^1.6.0"
  }
}
